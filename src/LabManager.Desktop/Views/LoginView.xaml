<UserControl x:Class="LabManager.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#f0f2f5">
        <Border Width="400" Height="300" Background="White" CornerRadius="10" Padding="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.1"/>
            </Border.Effect>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="LabManager.NET" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <TextBlock Text="Email"/>
                <TextBox Text="{Binding Email}" Margin="0,5,0,15" Padding="5"/>
                
                <TextBlock Text="Password"/>
                <!-- Binding PasswordBox is complex in MVVM without behaviors, we use a simple TextBox for demo or an attached property. Here we just use a TextBox for simplicity. -->
                <TextBox Text="{Binding Password}" Margin="0,5,0,20" Padding="5"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,10" HorizontalAlignment="Center"/>

                <Button Content="LOGIN" Command="{Binding LoginCommand}" 
                        Background="#0d6efd" Foreground="White" 
                        Padding="10" FontWeight="Bold" BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                    <Setter Property="Content" Value="Đang đăng nhập..."/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
