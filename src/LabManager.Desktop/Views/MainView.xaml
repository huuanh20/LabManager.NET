<UserControl x:Class="LabManager.Desktop.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:LabManager.Desktop.Views"
             xmlns:viewmodels="clr-namespace:LabManager.Desktop.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewmodels:PatientViewModel}">
            <views:PatientView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:TestOrderViewModel}">
            <views:TestOrderView/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#212529">
            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="LabManager.NET" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <Button Content="Quản lý Bệnh nhân" Command="{Binding ShowPatientCommand}" 
                        Background="Transparent" Foreground="White" BorderThickness="0" 
                        HorizontalContentAlignment="Left" Padding="20,15" FontSize="16"/>
                
                <Button Content="Chỉ định Xét nghiệm" Command="{Binding ShowTestOrderCommand}" 
                        Background="Transparent" Foreground="White" BorderThickness="0" 
                        HorizontalContentAlignment="Left" Padding="20,15" FontSize="16"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Border Grid.Column="1" Background="#f4f6f9">
            <ContentControl Content="{Binding CurrentViewModel}" Margin="20"/>
        </Border>
    </Grid>
</UserControl>
