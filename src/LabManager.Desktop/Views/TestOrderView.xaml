<UserControl x:Class="LabManager.Desktop.Views.TestOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Chỉ định Xét nghiệm" FontSize="28" FontWeight="Bold" Margin="0,0,0,15"/>
            
            <WrapPanel>
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Chọn Bệnh nhân"/>
                    <ComboBox Width="300" ItemsSource="{Binding Patients}" SelectedItem="{Binding SelectedPatient}" DisplayMemberPath="FullName" Padding="5"/>
                </StackPanel>
                <Button Content="Tạo Test Order" Command="{Binding CreateTestOrderCommand}" 
                        Padding="15,5" Background="#198754" Foreground="White" BorderThickness="0"
                        VerticalAlignment="Bottom"/>
                
                <Button Content="Tải lại Data" Command="{Binding InitCommand}" 
                        Padding="15,5" Background="#6c757d" Foreground="White" BorderThickness="0"
                        VerticalAlignment="Bottom" Margin="15,0,0,0"/>
            </WrapPanel>
        </StackPanel>
        
        <DataGrid Grid.Row="1" ItemsSource="{Binding TestOrders}" AutoGenerateColumns="False" 
                  IsReadOnly="True" GridLinesVisibility="Horizontal" AlternatingRowBackground="#f8f9fa">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên Bệnh nhân" Binding="{Binding PatientName}" Width="2*"/>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedOn, StringFormat=dd/MM/yyyy HH:mm}" Width="1.5*"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="1.5*"/>
                <DataGridTextColumn Header="Kết quả" Binding="{Binding RawResults}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
