<UserControl x:Class="LabManager.WPF.Views.TestOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Test Orders" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <Button Content="+ Add Order" Command="{Binding AddOrderCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="✏ Edit" Command="{Binding EditOrderCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="🗑 Delete" Command="{Binding DeleteOrderCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="↻ Refresh" Command="{Binding RefreshCommand}" Padding="12,6"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding TestOrders}"
                  SelectedItem="{Binding SelectedOrder}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Order #" Binding="{Binding OrderNumber}" Width="100"/>
                <DataGridTextColumn Header="Test Name" Binding="{Binding TestName}" Width="*"/>
                <DataGridTextColumn Header="Patient" Binding="{Binding PatientName}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Ordered At" Binding="{Binding OrderedAt, StringFormat=\{0:yyyy-MM-dd\}}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
