<UserControl x:Class="LabManager.WPF.Views.LabsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Labs" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <Button Content="+ Add Lab" Command="{Binding AddLabCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="✏ Edit" Command="{Binding EditLabCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="🗑 Delete" Command="{Binding DeleteLabCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="↻ Refresh" Command="{Binding RefreshCommand}" Padding="12,6"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Labs}"
                  SelectedItem="{Binding SelectedLab}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="*"/>
                <DataGridTextColumn Header="Contact Email" Binding="{Binding ContactEmail}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding ContactPhone}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
